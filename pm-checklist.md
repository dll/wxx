# wxx vOPC PRD 需求核对清单

> 核对对象：`docs/wxx-vopc-prd-v1.0.md`（WXX-vOPC-PRD-v1.0，2026-08-20）
> 核对日期：2026-08-21
> 核对范围：项目结构、前后端路由、权限模型、用户模型、审计/模型/文件基础能力及仓库规范。
> 结论性质：需求与现状差距核对，不代表功能已实现。

## 1. 总结结论

- **PRD 目标清晰**：vOPC 是计算机学院内授权用户使用的 AI 创业与真实产品孵化平台，核心是“项目负责人人类决策 + AI 虚拟员工 + 阶段门禁 + 可运行成果 + 用户验证/复盘”的闭环。
- **当前实现状态：未开始**。除 PRD 本身外，代码、Flutter 页面、Go 路由、Capability、模型实体、迁移文件中均未发现 `vopc`/`VOPC` 实现；不存在 `/vopc` 前端路由或 `/api/v1/vopc` 后端接口。
- **P0 属于新增业务域**，不是现有“智能体管理”“知识库”“竞赛”“毕设”等模块的简单拼装。至少需要新增项目域的数据模型、服务、接口、页面、权限和隔离策略。
- **已有可复用基础**：Flutter + Go/Gin + Dio/Provider、JWT、角色继承 + Capability、学院/归属字段、全局审计与 TraceID、通知、文件/文档解析、AI 模型接入与故障切换、Token 使用统计、SQLite/迁移机制。
- **P0 当前为阻塞状态**：没有可运行闭环，所有 P0 功能验收项均未满足；其中“计算机学院准入三层校验”“项目/文件/AI 上下文隔离”“人类审阅与审计”“风险审批”和“门禁状态机”是上线前硬阻塞。

## 2. PRD 目标与范围核对

| 项目 | PRD 要求 | 现状判断 | 结论 |
|---|---|---|---|
| 产品定位 | 通用创业与实验孵化，不局限软件或课程 | 现有系统主要是校园问答、办事、教育、就业、知识与智能体管理 | **差距**：需新增通用项目类型/交付物模型 |
| 用户边界 | 仅计算机学院已授权蔚小芯用户；外院、校外、游客拒绝 | `User` 有 `College`、`OwnerScope`、`OwnerID`；角色含 `guest`，但没有 vOPC 学院准入策略 | **P0 阻塞**：需后端接口、数据查询、前端入口三层校验，不能只隐藏菜单 |
| 身份模型 | 系统角色与项目角色分离；同一用户可跨项目担任不同角色 | 当前角色模型是全局角色继承；无项目成员关系模型 | **P0 差距**：新增项目角色与授权判断 |
| 真实交付 | 项目必须有可运行产品/服务、实验结果、用户反馈或失败证据 | 当前没有项目、成果版本、用户试点/反馈、结项复盘域 | **P0 阻塞** |
| AI 协作 | AI 是虚拟员工；任务、产出、审阅、退回、否决、版本和决策可追溯 | 有通用 Agent Hub/模型 API，但没有项目级 AI 岗位、AI 任务、审阅链和项目上下文 | **P0 阻塞** |
| 孵化阶段 | S0-S9 阶段和门禁；异常状态含冻结、暂停、终止等 | 现有流程记录不是 vOPC 项目状态机；无阶段门禁 | **P0 阻塞** |
| 经营/财务 | v1.0 不做复杂财务；保留成本、收入/收益、价值复盘边界 | 现有 Token 统计可复用，但没有项目成本/收益/复盘模型 | **P1/后续**；P0 至少需价值验证与失败复盘 |
| 展示范围 | 默认私有；经审核后仅学院内成果展厅，v1.0 不公开 | 无 vOPC 展厅/发布审核 | **P1 功能，P0 需确保默认私有和禁止越权展示** |

## 3. 现状证据与可复用能力

### 3.1 已确认存在

- 前端技术栈和路由集中在 `frontend/lib/config/router.dart`，已有 GoRouter、响应式 `NavigationRail`/`NavigationBar` 和主 Shell。
- 后端 API 统一挂在 `server/pkg/app/routes.go` 的 `/api/v1`，已有 JWT 保护的 `secured` 路由组。
- `server/internal/auth/capabilities.go` 已实现角色继承：`sys_admin → school_admin → college_admin → counselor/teacher/assistant → student_union → student`，但没有任何 `vopc.*` 能力。
- `server/internal/middleware/jwt.go` 已校验 Bearer JWT、账号状态、issuer，并注入用户的角色、归属范围等上下文；该机制可作为 vOPC 鉴权基础。
- `server/internal/middleware/audit.go` 已对请求写入 `audit_logs`，包含 action、resource、trace、用户和结果码；但 vOPC 关键业务事件、AI 产出版本和决策理由仍须业务级审计，不能只依赖 HTTP 请求日志。
- `server/internal/llm/failover.go` 已有超时和主备模型切换；PRD 要求的 AI 任务失败不阻塞项目，还需任务级失败状态、重试策略和成本记录。
- `server/internal/repository/token_usage_repo.go` 已有用户/模型 Token 使用记录与统计；无法直接替代 vOPC 的项目、阶段、AI 角色、任务维度成本统计。
- `server/internal/handler/upload_handler.go` 已有文档/图片/媒体格式上传和解析，但其目标是知识库入库；不可直接视为成果仓库，需补充项目成员权限、版本、来源、许可、可见范围和私有 URL 校验。
- `server/internal/model/entity.go` 的 `User` 已有 `College`、`OwnerScope`、`OwnerID`、`Role`、`Status` 等字段，适合做学院授权初始判定；字段口径和学院标识必须在实施前确认。
- 仓库开发规范要求 Flutter + Go/Gin + JWT/RBAC、SQLite/FTS、Eino/自研 Agent Hub、模型 API 复用，不强制 Docker/集群；vOPC PRD 与这些约束一致。

### 3.2 未发现实现

- 无 `vopc` 前端目录、页面、Provider、Service 或路由。
- 无 `/api/v1/vopc` 路由、Handler、Service、Repository 或 DTO。
- 无 `vopc.*` Capability，也无学院授权用户专用的 vOPC middleware/查询 scope。
- 无 `vopc_projects`、`vopc_project_members`、`vopc_tasks`、`vopc_artifacts`、`vopc_milestones`、`vopc_decisions`、`vopc_reviews`、`vopc_risks`、`vopc_events` 等迁移/实体。
- 无项目级 AI 角色/任务/上下文隔离、AI 产出审阅与版本链。
- 无 `/vopc`、`/vopc/explore`、`/vopc/create`、`/vopc/mine`、`/vopc/projects/:id/*` 等页面。
- 无 vOPC 项目级测试数据、验收用例或试点运营后台。

## 4. P0 可用孵化闭环清单

状态约定：`[ ] 未实现`，`[~] 有可复用基础但未满足 vOPC`，`[x] 已满足`。

### 4.1 入口、准入和导航

- [ ] 新增 Web/桌面端 `/vopc` 一级入口和 `rocket_launch` 图标。
- [ ] 移动端 vOPC 一级可直达；6 项拥挤时按 PRD 合并“服务”为“更多”，不能降低 vOPC 可达性。
- [ ] 未登录、游客、外院、校外或未通过学院归属校验用户：前端不可见且后端 `/api/v1/vopc/*` 明确拒绝。
- [ ] 对 vOPC 入口、每个 API、项目列表/详情查询分别验证学院授权，证明不是仅靠菜单隐藏。
- [ ] 已授权的 student、student_union、teacher、assistant、counselor、college_admin、school_admin、sys_admin 均可按 PRD 基础能力创建/参与；不能错误限定为学生。

### 4.2 项目创建与基本工作台

- [ ] 自拟项目可保存草稿；至少包含名称、问题、目标用户/甲方、形态、类型、成果、周期、验证/验收指标、风险等级、数据类型和导师/资源需求。
- [ ] AI 引导只能辅助填写/澄清，不能代替主理人提交；字段缺失时不得提交孵化。
- [ ] 项目类型覆盖 PRD 的软件/AI、内容知识、校园服务、创新创业、科研实验、公益实践、教学改革、自由探索，或允许可审计的自由扩展。
- [ ] 工作台展示阶段、完成度、真人与 AI 成员、下一里程碑、待决策、风险、最新产出和活动日志。
- [ ] 项目成员关系与系统身份分离；实现主理人、联合主理人、成员、导师、评审者、资源支持者、平台运营者等角色的最小权限。
- [ ] 默认 `private`；项目公开到学院范围必须显式申请/审核，禁止默认公开。

### 4.3 任务、AI 团队和产出

- [ ] 任务支持负责人（真人或 AI）、依赖、优先级、截止时间、验收标准、状态、评论和附件。
- [ ] 默认启用 4 个 AI 岗位，主理人可调整，最多同时启用 6 个；至少覆盖项目经理、市场用户、产品方案、执行专家、测试评估、运营增长、文档知识中的可配置组合。
- [ ] AI 任务记录输入、项目上下文范围、模型/提供商、产出版本、耗时、Token/成本、审阅动作和最终状态。
- [ ] AI 产出必须支持接受、修改、退回、否决、版本化；未经人审阅不得自动进入项目已批准事实/知识库。
- [ ] AI 失败、超时、重试均有明确任务状态，不阻塞其他项目任务；成本额度超限需主理人确认或平台授权。
- [ ] 成果仓库支持 PRD 要求的 Markdown/PDF/Word/Excel/图片/压缩包/链接/代码仓库/数据集引用，并记录版本、作者、来源、许可、可见范围。
- [ ] 项目上下文按项目隔离；AI 只能读取当前用户在项目内有权访问的内容，跨项目复用必须授权和脱敏。

### 4.4 决策、里程碑、评审与复盘

- [ ] 决策中心记录背景、选项、AI 建议、影响、截止时间、决策人、理由和结果。
- [ ] S0-S9 阶段状态和 `rejected`、`change_pending`、`paused`、`terminated`、`archived`、`risk_frozen` 异常状态有明确状态转换表。
- [ ] 里程碑配置必交产出、评分量表、提交、点评、通过/退回；未满足门禁不可直接跳阶段，豁免需理由和审批记录。
- [ ] 甲方需求项目不得跳过 S2、S5、S6；自拟项目不得跳过真实用户验证、产品交付和迭代记录。
- [ ] 关键节点必须由人确认：范围/风险、方案选择、AI 审阅、真实试点、发布、结项/转向/终止和复盘。
- [ ] 结项支持继续、转向、暂停、终止、结项；终止必须有原因、反馈、验证证据和经验复盘。

### 4.5 权限、风险与审计

- [ ] 新增并评审 `vopc.read`、`vopc.project.create/manage/join`、`vopc.mentor.review`、`vopc.milestone.review`、`vopc.resource.offer`、`vopc.publish.request`、`vopc.audit`、`vopc.risk.manage`、`vopc.analytics.read` 等 Capability。
- [ ] API 同时执行 JWT、Capability、学院归属、项目成员关系、字段级可见性和业务审计；不得仅由 Handler 内部前置判断替代路由鉴权。
- [ ] R0 自动进入；R1 告知/基础审核；R2 导师/管理员审核和安全检查；R3 默认禁止或专项审批；真实用户测试、敏感数据、外部发布和资金相关场景自动触发规则。
- [ ] 私有项目、私有成果和私有文件的详情、下载 URL、查询列表均做授权过滤；不能因猜测 ID 或直链访问越权。
- [ ] AI 调用、人工修改、决策、状态变化、审批/豁免、风险冻结/解冻和成果展示审核都有不可抵赖的审计事件。
- [ ] 个人信息、学籍、心理、健康等敏感数据进入模型前拦截或提示；代码/内容来源、许可和人工审阅状态可追溯。

## 5. P1 清单

- [ ] 申请加入、学院内伙伴/技能匹配、主理人审批和工作量信息。
- [ ] 导师库、导师邀请、评审日、资源需求/资源方意向及处理结果。
- [ ] 成果展厅、项目路演、Demo、问答记录和学院内授权展示审核。
- [ ] 代码仓库、竞赛、毕设、个人档案、作品集和学院成果库联动。
- [ ] 更多 AI 岗位、项目类型模板与运营活动（训练营、开放日、Demo Day、失败复盘专栏）。
- [ ] 校内真实用户试点审批、告知同意、反馈/缺陷/工单闭环。
- [ ] 成果包导出：摘要、成员贡献、关键决策、里程碑、成果目录、试点数据、风险声明、复盘报告；导出必须含操作者、角色、时间、traceId 和水印策略。
- [ ] 运营统计：创建数、有效立项率、首任务完成率、阶段转化、协作率、AI 产出修改/退回率、真实验证数、复盘率、导师响应、展示/资源对接成功数、项目/用户/阶段成本。

## 6. 依赖与风险

### P0 硬依赖

1. **学院授权口径**：必须确定用户 `College`/`OwnerScope`/`OwnerID` 的正式取值、外院/校外/游客判定规则、SSO/导入数据是否可靠；否则无法完成准入验收。
2. **项目权限矩阵**：系统角色、项目角色、项目成员关系、导师/评审邀请关系要先定稿，否则 API 和数据查询容易形成越权。
3. **项目状态机与门禁**：S0-S9、异常状态、豁免流程和人审责任人必须形成状态转换表，再进行前后端实现。
4. **数据与上下文隔离方案**：项目、成员、成果、AI 草稿/批准事实、文件下载权限需要同一 scope 设计，不能事后补权限。
5. **AI 任务运行模型**：需要决定同步/异步队列、超时/重试、失败状态、模型额度、项目成本归属和上下文快照；现有通用对话流不足以直接覆盖。
6. **真实用户/敏感数据合规**：R1-R3 审核主体、告知同意、数据最小化、外部发布和知识产权/许可责任需学院确认。
7. **文件存储和部署约束**：成果仓库的存储位置、备份/恢复、私有 URL、大小/类型限制要与现有部署一致，不能默认引入独立对象存储或 Docker 集群。

### 主要风险

- **范围膨胀风险（高）**：PRD 同时覆盖创业、研发、运营、经营、展厅和多角色治理；若不冻结 P0，容易把 P1/P2 一并塞入首版。
- **权限越权风险（高）**：学院范围、项目成员权限、导师/评审授权和字段级可见性是多层 scope，单一全局 Capability 不足。
- **AI 数据泄露风险（高）**：项目间上下文串读、未批准草稿进入事实库、私有文件被模型/直链读取都可能造成严重泄露。
- **状态绕过风险（高）**：若只在前端禁用按钮，客户端可直接调用提交/发布/结项 API 绕过 S2/S5/S6 门禁。
- **“真实产品”验收歧义（高）**：不同项目类型交付物不同，必须为每类定义最小证据，否则项目可能以文档代替交付。
- **模型成本与失败风险（中高）**：现有 Token 统计按用户/时间统计，未按项目/阶段/AI 角色结算；重试和流式任务可能产生隐性成本。
- **文件和知识库混用风险（中高）**：复用知识库上传流程可能误把项目私有成果公开入库或套用不适用的 roleScope。
- **用户运营负担风险（中）**：20-30 个项目、多角色导师/评审和 R2 审核会产生较高人工运营量，需试点前估算 SLA。
- **验收性能风险（中）**：PRD 要求列表/工作台 P95 < 2 秒（不含模型调用）；项目活动、权限过滤和统计查询若无索引/分页可能不达标。

## 7. P0 验收门禁（建议直接转测试用例）

### 7.1 正向场景

1. 一个已授权学生创建草稿，补齐字段后提交；系统生成项目、主理人项目角色、默认 4 个 AI 岗位和初始 S0/S1 门禁。
2. 教师、辅导员、教辅、学生会和学院管理员分别登录，均可按授权规则创建/参与项目；验证系统身份与项目身份分离。
3. 主理人邀请学院内用户，成员接受后访问项目；成员不能修改主理人、全局可见性或高风险审批。
4. 主理人给真人和 AI 分配任务；AI 任务超时/失败后可重试，其他任务和项目工作台仍可用。
5. AI 生成产出后，主理人分别执行接受、修改、退回、否决，检查版本、审阅动作、模型和成本记录。
6. 未满足里程碑门禁时提交下一阶段返回明确拒绝；有理由的豁免留下审批/审计记录。
7. 甲方需求项目尝试跳过 S2、S5、S6，应被后端拒绝；自拟项目未提交真实验证/交付证据时不能结项。
8. R2/R3 项目创建或提交时进入审核/拦截；风险冻结后项目写操作和发布均受限，并记录处置理由。

### 7.2 负向权限场景

1. 外院、校外、游客、未登录用户不能看到入口、访问 API、查询大厅或猜测项目 ID。
2. 非成员不能读 private 项目、任务、AI 对话、决策、成果、文件下载 URL 或活动日志。
3. 学院内 `college` 摘要可见不等于代码、数据集、商业敏感信息可见；字段级限制必须生效。
4. 导师仅能访问被邀请项目；评审者仅能访问分配的里程碑材料；资源支持者不能读取不相关项目内容。
5. 普通成员不能变更主理人、项目可见性、风险等级、发布状态或结项决定。
6. 管理员的冻结、解冻、审核、恢复等操作必须有操作者、理由、时间、traceId 和结果。

### 7.3 性能、可靠性和安全

- 项目列表、我的项目、工作台在代表性 20-30 项目试点数据下 P95 < 2 秒（不含模型调用），带分页和必要索引。
- AI 任务超时、失败、重试、额度不足均返回明确状态；单任务失败不阻塞项目其他操作。
- 文件上传、权限校验或审计写入失败不得返回虚假成功。
- 通过直接 API 调用、篡改 ID、过期 JWT、不同系统角色和不同项目角色执行越权回归测试。
- 敏感数据进入模型有拦截/提示；私有文件不可通过未授权 URL 访问；项目归档/删除按保留策略处理上下文和文件。
- 结项成果包能复现关键决策、成员贡献、门禁结果、试点证据、风险声明和复盘。

## 8. 关键阻塞项

1. **vOPC 全部 P0 功能尚未实现**，目前不存在可供验收的入口、接口、数据或工作台。
2. **学院授权边界未落到 vOPC 专用策略**：现有角色继承不等价于“计算机学院授权用户”，不能直接复用作为准入结论。
3. **项目级 RBAC/Capability + 数据 scope 尚未设计**：尤其是成员、导师、评审、资源支持者和平台运营者。
4. **项目状态机/里程碑门禁未定义为可执行契约**：若先做页面，后端容易被绕过。
5. **项目级 AI 上下文、草稿/批准事实、文件访问和成本归属方案缺失**：这是数据安全和费用控制的上线前阻塞。
6. **真实用户试点与 R2/R3 合规审批主体、告知同意、外部发布及知识产权责任未确认**。
7. **成果仓库存储、备份、私有 URL 和版本策略未确认**，现有知识库上传能力不能直接替代。

## 9. 推荐实施顺序

1. 先冻结 P0 的状态转换、项目角色/Capability、学院准入规则、字段可见性、风险矩阵和最小交付证据。
2. 设计并评审 `vopc_*` 数据表、迁移回滚、索引、审计事件和项目上下文隔离边界。
3. 先做后端最小闭环：项目草稿/提交、成员、任务、成果、里程碑、决策、复盘；所有状态转换在服务端强制。
4. 接入项目级 AI 角色/任务时，先落审阅、版本、失败状态、额度与成本，再做流式体验和更多岗位。
5. 再接 Flutter `/vopc`、我的项目和工作台，入口显示必须由能力与学院准入结果驱动。
6. 用正/负向权限、跨项目泄露、状态绕过、私有文件直链、R2/R3、失败重试和 P95 用例验收后，才进入 P1 展厅/伙伴/资源生态。

## 10. 核对记录

- 本轮仅读取和分析项目及文档；未修改任何源码。
- 根目录此前不存在 `pm-checklist.md`，本轮按要求新建；后续核对应在本文件基础上增量更新，避免覆盖已有结论。
