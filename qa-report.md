# wxx vOPC 最终回归测试报告

- 测试日期：2026-08-21
- 测试范围：vOPC 项目、任务、决策、成员邀请、成果版本、正式里程碑及应用路由/迁移回归
- 最终结论：**通过（PASS，视觉验证按用户指示豁免）**

## 1. 自动化门禁结果

| 门禁 | 命令/范围 | 结果 |
|---|---|---|
| Go handler + app | `go test ./internal/handler ./pkg/app -count=1` | PASS：handler 73.921s，app 71.733s |
| Go 静态检查 | `go vet ./internal/handler ./pkg/app` | PASS（同一命令链 exit 0） |
| Flutter 定向分析 | vOPC Provider、页面、路由、API 常量、能力映射及测试 | PASS：No issues found（130.0s） |
| Flutter 全量测试 | `flutter test` | PASS：12 项全部通过 |
| 差异格式检查 | `git diff --check` | PASS |
| 新库迁移烟测 | 隔离 SQLite 数据库启动 | PASS：001—099 共 96 个迁移文件执行完成，包含 097/098/099 |
| 真实 API 准入烟测 | 隔离数据库、受控 JWT 调用 `/api/v1/vopc/access` | PASS：`allowed=true`、`college_id=cs` |

## 2. 重点回归覆盖

- JWT、Capability、计算机学院准入和项目关系策略。
- 私有项目访问与跨项目资源防枚举（不存在或无权资源统一 404）。
- 项目创建、提交以及 S0—S9 合法状态推进。
- 任务创建和状态流转。
- 决策创建及 `pending -> resolved/cancelled`。
- 学院 active 用户邀请、接受/拒绝和项目角色关系。
- 成果元数据与版本引用；服务端不执行任意文件。
- 里程碑材料提交、指定评审/平台评审、通过/退回及证据要求。
- Provider 对服务端 ID、409 和伪成功响应的处理。
- 写操作事务与业务审计原子提交。

## 3. 状态码与失败语义

实现及测试覆盖明确的 `401/403/404/409/422` 语义；跨项目枚举不泄露目标资源，冲突和字段/证据错误不会被前端报告为成功。

## 4. 豁免项

用户于 2026-08-21 15:52 明确要求跳过：

1. 真实浏览器/设备页面截图验证；
2. 基于视觉验证发现问题后的修复。

因此本报告不声称完成真实视觉验收，也未把该豁免项作为自动化回归失败。建议发布前另行执行桌面与移动端视觉烟测。
