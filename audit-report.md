# wxx vOPC 最终代码审核报告

- 审核日期：2026-08-21
- 审核范围：当前 vOPC 097—099 迁移、Go Handler/路由/策略、Flutter Provider/UI、回归测试与交付文档
- 结论：**允许提交合并；生产发布前仍建议补做被豁免的视觉烟测。**

## 1. 已关闭的关键问题

- Flutter 已接入真实 API/Provider，不使用本地假数据或伪成功。
- `/vopc` 路由与入口受登录状态和 `vopc.read` 能力控制；后端继续执行 JWT、Capability、学院归属和项目关系纵深校验。
- 创建、提交、决策、邀请、成果版本和里程碑写操作使用事务，业务事件与状态变更原子提交。
- 状态转换检查当前状态及受影响行数，非法/重复操作返回冲突或校验错误，不写伪审计事件。
- 已实现任务、决策、邀请、成果版本、里程碑提交/评审闭环和 S0—S9 合法推进。
- 跨项目资源使用项目策略校验并按防枚举原则返回 404。
- 099 仅保存成果元数据和安全引用，不提供任意文件执行路径。
- 097/098 既有语义未回退，新增交付能力由 099 兼容迁移承载。
- 学院标识已由 `VOPC_COLLEGE_ID` 配置，不再仅依赖散落硬编码。

## 2. 权限与治理判断

- 全局 Capability 仅表示可尝试动作，具体操作仍由项目 owner/active membership/project role/指定 reviewer 约束。
- 成员邀请限制为目标学院 active 用户，并保留 pending/accepted/rejected 状态及审计。
- 里程碑仅允许主理人提交；指定评审或具备平台评审能力的角色评审。
- AI 仅提供建议和执行辅助，不代替 OPC 执行资金、合同、人事、重大技术路线、对外发布等重大事项。

## 3. 验证证据

- `go test ./internal/handler ./pkg/app -count=1`：PASS。
- `go vet ./internal/handler ./pkg/app`：PASS。
- Flutter 定向 `analyze`：PASS，No issues found。
- `flutter test`：PASS，12 项全部通过。
- `git diff --check`：PASS。
- 隔离 SQLite 新库迁移：001—099 全链执行完成。
- 受控 JWT 调用真实 `/api/v1/vopc/access`：PASS。

## 4. 已知豁免与剩余风险

用户明确跳过真实浏览器/设备截图验证及其后续修复。因此：

- 未形成桌面/移动端视觉证据；
- 未验证复杂屏幕尺寸下的溢出、焦点、滚动和弹窗布局；
- Web 构建产物曾生成，但宿主在最终成功提示前终止进程，不能替代正式视觉验收。

这不影响本次代码提交结论，但正式发布前建议补做一次真实设备/浏览器视觉烟测。
