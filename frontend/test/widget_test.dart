import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('应用启动冒烟测试', (WidgetTester tester) async {
    // TODO: 补充完整的冒烟测试（需要 Mock SharedPreferences）
    expect(1 + 1, equals(2));
  });
}
