name = "wxx-agent"
compatibility_date = "2024-01-01"
pages_build_output_dir = "build/web"

# TURSO_DB_URL / TURSO_DB_TOKEN / GO_BACKEND_URL / JWT_SECRET 一律走 Pages 加密环境变量
# （wrangler pages secret put），不要写在 [vars] 里：
#   1. [vars] 是明文，会随仓库一起提交，等于凭据泄露
#   2. [vars] 优先级高于加密变量，会覆盖 secret store 里的值
