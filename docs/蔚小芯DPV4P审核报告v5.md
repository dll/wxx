# 蔚小芯 DPV4P 审核报告 v5

> **审核日期**：2026-08-02  
> **审核范围**：Go/Gin 后端（server/）架构 + 安全 + 知识管道 + 代码质量 + **功能实现度** + **上线材料就绪度**  
> **基于**：v4 报告 + 当前 HEAD 代码核验 + `docs/蔚小芯角色功能.md`（v5.2）+ `docs/蔚小芯角色功能所需材料.md`（v2.0）  
> **严重级别**：阻断（Blocking）> 警告（Warning）> 备注（Note）

---

## 目录

1. [修复进展总览](#1-修复进展总览)
2. [架构合规](#2-架构合规)
3. [安全审计](#3-安全审计)
4. [知识管道（Context Engine）审计](#4-知识管道context-engine审计)
5. [功能实现度审计（对照角色功能.md）](#5-功能实现度审计对照角色功能md)
6. [上线材料就绪度审计（对照所需材料.md）](#6-上线材料就绪度审计对照所需材料md)
7. [残余问题与修复路线图](#7-残余问题与修复路线图)
8. [综合评分与结论](#8-综合评分与结论)

---

## 1. 修复进展总览

### 1.1 v4 遗留 8 项核验状态（对照当前 HEAD）

| # | ID | 问题 | v4 状态 | v5 状态 |
|---|----|------|---------|---------|
| 1 | NEW-B1 | `chat_handler.go` 直接导入 `repository`（架构阻断） | ⚠️ 遗留 | **✅ 已修复**（不再 import repository，仅测试文件与 `campus_handler.go` 持 repo 引用） |
| 2 | NEW-W1 | 12 处 handler `err.Error()` 泄露（7 文件） | ⚠️ 遗留 | **⚠️ 部分修复**（`feedback_handler.go` 已清零；6 文件残余，见 §3.2） |
| 3 | NEW-W2 | `crypto.go` 静默绕过（无密钥时明文存储） | ⚠️ 遗留 | **✅ 已修复**（`crypto.go:23` `log.Printf("[WARN] ...明文存储")`） |
| 4 | NEW-W3 | FTS5 查询构建器不转义 `AND/OR/NOT/NEAR` | ⚠️ 遗留 | **✅ 已修复**（`kb_repo.go:252` `escapeFTS` 剥离运算符与括号；`buildLooseQuery`/`escapeQuery` 均经 `escapeFTS`） |
| 5 | NEW-W4 | `/documents/formats`、`/kb/formats` 无 RBAC | ⚠️ 遗留 | **✅ 已修复**（`app.go:1022,1026` 加 `RequireAnyCapability(UnionKBSubmit, CounselorKBWrite)`） |
| 6 | NEW-W5 | `/uploads/feedback/:filename` 在 secured 组外 | ⚠️ 遗留 | **✅ 已修复**（`app.go:904` 移入 `secured` 组） |
| 7 | NEW-W6 | `twin_handler.go` 使用 `gin.H{}` + `err.Error()` | ⚠️ 遗留 | **✅ 已修复**（改用 `model.ErrorResponse` + `TraceID`，`twin_handler.go:29,36`） |
| 8 | NEW-W7 | `teacher_handler.go:43` 使用 `gin.H{}` 缺 TraceID | ⚠️ 遗留 | **✅ 已修复**（`teacher_handler.go:45` 改用 `model.ErrorResponse` + `TraceID`） |

### 1.2 v4 已修复项维持确认

NEW-W8（6 处裸 `return err`）、NEW-W9（`auth_service.go` 导入 middleware → 提取 `jwtutil`）、NEW-N1（`kb_repo.go:1110` defer rows.Close）、NEW-N2（qa_agent 置信度动态计算）、NEW-N3（audit 优雅关闭）——v4 已修复，本次核验未见回退。

### 1.3 v5 新增发现

| ID | 类别 | 问题 | 严重级 |
|----|------|------|--------|
| NEW-V1 | 文档 | 能力授权数量不符：`capabilities.go` 实际 **104** 个 `Capability` 常量，《角色功能.md》声称 **77** 项 | **警告** |
| NEW-V2 | 产品 | 规划功能大量未实现：角色功能.md 中约 **40+ 项** ❌ 未实现（§5），现有教师/教辅接口多为 mock 兜底数据 | **警告** |
| NEW-V3 | 产品 | 上线材料就绪度低：所需材料.md v2.0 全部行 ☐ 未勾选，P0 数据源（课表/成绩/心理/考勤）未就绪 | **警告** |
| NEW-V4 | 安全 | ShouldBindJSON 校验错误仍带 `err.Error()` 前缀（30+ 处），泄露 schema 细节 | **警告** |
| NEW-V5 | 代码质量 | `assistant_handler.go` 等返回硬编码 mock 数据（`conflicts: []gin.H{}`、`results: []gin.H{{"name":"张明"...}}`），无真实数据源接入 | **备注** |

---

## 2. 架构合规

### 2.1 总体评估

| 标准 | v3 状态 | v4 状态 | v5 状态 |
|------|---------|---------|---------|
| handler → service → repository 单向分层 | 合规 | **1 处违反**（chat_handler） | **✅ 合规**（0 处） |
| 禁止 handler 导入 `repository` | 已修复 | 已违反 | **✅ 合规**（`chat_handler.go` 已迁移） |
| 禁止 handler 导入 `llm` / `agent` / `context_engine` | 合规 | 合规 | **✅ 合规**（0 处） |
| service 导入规范 | 通过 | 警告（已修复） | **✅ 合规** |
| `model.ErrorResponse` 一致使用 | 部分通过 | 部分通过 | **✅ 通过**（twin/teacher 已修正） |

### 2.2 结论

v4 唯一的架构违规（NEW-B1）已消除，handler 层现在保持「只依赖 service，不直接触碰 repository」的分层纪律。架构合规维度恢复满档。

---

## 3. 安全审计

### 3.1 修复验收

| v4 ID | 问题 | v5 状态 |
|-------|------|---------|
| NEW-W2 | AES 静默绕过 | **✅ 已修复**（`crypto.go:23` WARN 日志） |
| NEW-W3 | FTS5 运算符注入 | **✅ 已修复**（`escapeFTS` 剥离 `AND/OR/NOT/NEAR/()`） |
| NEW-W4 | formats 路由缺 RBAC | **✅ 已修复** |
| NEW-W5 | feedback 路由未认证 | **✅ 已修复**（移入 secured 组） |
| NEW-W1 | err.Error() 泄露 | **⚠️ 部分修复**（见下） |

### 3.2 警告级 — NEW-W1 残余清单（当前 HEAD 核验）

`feedback_handler.go` 已清零（v4 的 240/276 两处不再匹配）。以下仍存在 `Message: ... + err.Error()` 泄露：

| 文件 | 行号 | 泄露内容 |
|------|------|----------|
| `graduation_handler.go` | 147 | `"参数错误: " + err.Error()` |
| `model_config_handler.go` | 65 | `"参数校验失败: " + err.Error()` |
| `process_record_handler.go` | 38, 99 | `"查询办事记录失败："` / `"参数错误："` |
| `session_handler.go` | 49, 179 | `"查询会话列表失败："` / `"参数错误："` |
| `study_plan_handler.go` | 137 | `"查询当前校历失败: "` |
| `voice_handler.go` | 131 | `"请求参数错误："` |

**NEW-V4（同类问题）**：30+ 处 `ShouldBindJSON` 校验错误带 `err.Error()` 前缀，分布见 `auth_handler.go`（37/133/204/241/276）、`campus_handler.go`（57/149/187 等约 10 处）、`forecast_handler.go`（45/210 等 8 处）、`admin_handler.go`（107/292/329/365/457/563/705）、`kb_handler.go`（161/282）、`emotion_handler.go`（30/178）、`education_mental_health_handler.go`（158/419/731）等。

**修复建议**：统一替换为通用消息（如 `"请求参数错误"`），细节走 `log.Printf`；校验失败仅返回「字段类型/格式错误」。

### 3.3 SQL 参数化复核

| 文件 | v4 结论 | v5 复核 |
|------|---------|---------|
| `student_features_repo.go` | 动态 WHERE `fmt.Sprintf` | ⚠️ 维持（未核验改动，建议列入路线图复核） |
| `graduation_repo.go` | 同上 | ⚠️ 维持 |
| `user_repo.go` | 白名单校验，安全 | ✅ 安全 |
| `kb_repo.go` | 排序字段白名单，安全 | ✅ 安全 |

---

## 4. 知识管道（Context Engine）审计

### 4.1 总体评估

| 检查项 | v3 | v4 | v5 |
|--------|----|----|----|
| 来源附加 `sources[]` | 通过 | 通过 | ✅ 通过 |
| 禁止编造提示词 | 通过 | 通过 | ✅ 通过 |
| 范围过滤（scope/role/status） | 通过 | 通过 | ✅ 通过 |
| 兜底处理 | 通过 | 通过 | ✅ 通过 |
| 管道顺序（结构化→FTS→拼装→LLM） | 部分 | 通过 | ✅ 通过 |
| LLM 调用前检查上下文 | 部分 | 通过 | ✅ 通过 |

### 4.2 结论

知识管道维持 v4 的满分状态，无新增问题。`escapeFTS` 的补齐同时消除了 FTS5 注入面（§3.1），上下文检索管道保持「结构化优先 → FTS 兜底 → 去重合并 → LLM」的健康顺序。

---

## 5. 功能实现度审计（对照角色功能.md）

> 对照基准：`docs/蔚小芯角色功能.md` v5.2（2026-05-17）。本审计以当前 HEAD 后端 handler/service 与前端路由为核验依据。

### 5.1 能力授权体系核对

| 项 | 文档声称 | 代码实际 | 结论 |
|----|----------|----------|------|
| capability 数量 | 77 项 | **104 个常量**（`server/internal/auth/capabilities.go`） | **NEW-V1**：文档滞后，需更新 |

### 5.2 各角色功能完成状态（抽样核验）

| 角色 | 文档状态 | 代码核验 | 结论 |
|------|----------|----------|------|
| 学生 | 智能问答/知识大厅/办事流程/语音/会话/导出/反馈/校歌/广播/讲座/活动/志愿/专注模式/校历 已完成 | 均有对应 handler 与前端路由 | ✅ 一致 |
| 学生 | 数字人导师/个人数字孪生/新生大学规划/成长路径/今日速览/学习日记/心理陪伴/思政学习/课程地图/课程学情/竞赛匹配/学伴/模拟面试/问答广场/排行榜/热点关注/站内私聊/办事流程增强 等 40+ 项 | **❌ 未实现**（无对应接口或为占位） | ⚠️ 与文档一致（文档本身标注未实现） |
| 辅导员 | 情感预警/告警处理/会话查看/知识 CRUD/知识审核/学生列表 | 有对应实现 | ✅ 一致 |
| 辅导员 | AI 今日关注/班级学情日报/预测预警/干预方案/谈心谈话/话术推荐/思想档案/班级画像/流程编辑 | **❌ 未实现**（无真实接口） | ⚠️ 与文档一致 |
| 教师 | 今日授课概览/备课/出题/课堂互动/批改/热力图/反思/风格分布/答疑 | `teacher_handler.go` 全部为 **mock 兜底数据**（`data_source: "fallback"`），`svc != nil` 时走 LLM 尝试、失败回落硬编码 | **NEW-V2**：骨架完成、无真实数据源 |
| 教辅 | 排课检测/毕业审核/考试编排/模板库/信息查询 | `assistant_handler.go` 返回硬编码数据（如 `conflicts: []gin.H{}`、`results: []gin.H{{"name":"张明"...}}`） | **NEW-V2 / NEW-V5**：mock |
| 管理员 | 情感/知识/用户/审计/质量看板/全局配置 | 有对应实现 | ✅ 一致 |

### 5.3 功能实现度小结

- **已完成基线**：智能问答、知识库、语音、会话、导出、反馈、校园文化 5 项、情感预警、权限体系（104 capability）、各管理员 CRUD/看板——与 v4 报告一致，构成可上线基座。
- **规划未落地**：角色功能.md 中 **40+ 项** AI 原生特色功能（学生 20+、辅导员 10+、教师 9、教辅 3、社区 5）仍为规划态。教师/教辅端虽有 handler，但为 **mock fallback**，未接真实数据源。
- **建议**：功能实现度属于产品路线（P1 全局 TOP10 协同功能组），不作为安全/架构阻断项，但应在 v5 报告中显式披露，避免「有页面=已实现」误判。

---

## 6. 上线材料就绪度审计（对照所需材料.md）

> 对照基准：`docs/蔚小芯角色功能所需材料.md` v2.0（2026-07-23）。全部功能行仍为 **☐ 未勾选**。

### 6.1 P0 材料就绪情况（核心功能）

| 功能 | 所需材料 | 就绪度 | 缺口 |
|------|----------|--------|------|
| 文本对话（SSE） | 大模型 API Key + System Prompt | 🟡 部分 | Key 已有配置（智谱/DeepSeek/讯飞），但生产凭据管理未见轮换/审计 |
| 知识大厅/搜索 | 知识库资源 ≥50 条 | 🟡 部分 | 预置 3 条（奖学金/转专业/毕业 FAQ），距 50 条目标缺口大 |
| 学生列表/导入 | 学生 Excel 花名册 | 🔴 未就绪 | 无正式花名册提交记录 |
| 情感预警 | 学生行为/心理数据源 | 🔴 未就绪 | 无对接凭证/数据流 |
| 谈心谈话 | 谈话记录（每次更新） | 🔴 未就绪 | 无历史记录 |
| 排课检测/毕业审核/考试编排 | 课程安排表/毕业生名单/考试安排 | 🔴 未就绪 | 无对接 |
| 教师备课/出题/批改 | 教学大纲/历年考题/批改标准 | 🔴 未就绪 | 无材料（可脱敏样本均未提供） |
| 隐私协议 | 法务审核版本 | 🔴 未就绪 | 无文档 |
| 语音 ASR/TTS | 讯飞 API 配置 | 🟡 部分 | 配置项存在，生产可用性未验证 |

### 6.2 就绪度结论

- **材料就绪度整体偏低**：核心业务数据源（课表/成绩/心理/考勤/花名册/考题/排课）全部依赖外部系统或人工整理，目前无任何一份 P0 材料在文档中勾选就绪。
- 材料缺口与 §5 功能未实现互为因果：**功能没实现 → 不产生材料需求**；**材料没就绪 → 功能即便有页面也是空壳**。
- 最小可用上线建议：以「智能问答 + 知识库（≥50 条核心政策/FAQ）+ 学生导入 + 情感预警」为 P0 闭环，优先补齐这四类材料。

---

## 7. 残余问题与修复路线图

### 7.1 按优先级排序

| # | ID | 组件 | 问题 | 估时 | 严重级 |
|---|----|------|------|------|--------|
| 1 | NEW-W1 残余 | 6 个 handler | `err.Error()` 泄露（graduation/model_config/process_record/session/study_plan/voice） | 1.5h | **警告** |
| 2 | NEW-V4 | 30+ 处 handler | ShouldBindJSON 校验消息带 `err.Error()` | 2h | **警告** |
| 3 | NEW-V1 | `capabilities.go` | 文档 77 vs 实际 104，需同步《角色功能.md》 | 0.5h | **警告** |
| 4 | NEW-V3 | 数据源接入 | P0 材料：花名册/知识库≥50/心理/课表数据 | 规划级 | **警告** |
| 5 | NEW-V2 | 教师/教辅端 | mock → 真实 LLM + 数据源（teacher_handler 9 接口、assistant_handler） | 规划级 | **警告** |
| 6 | NEW-V5 | assistant_handler | 硬编码返回移除，改接入真实服务 | 2h | **备注** |
| 7 | SQL 动态 WHERE | student_features_repo / graduation_repo | 参数化改造（v4 遗留未核验） | 2h | **备注** |

> v4 遗留 8 项 → **7 项已修复，1 项（NEW-W1）部分修复**。当前无阻断级遗留。

### 7.2 建议实施顺序

1. **短期（<1 天）**：清零 NEW-W1 残余 6 处 + NEW-V4 校验消息；同步 capabilities 文档（NEW-V1）。
2. **中期（P1）**：按「全局 TOP10 协同功能组」优先落地 —— 个人数字孪生 → 今日速览/今日关注 → 问答广场+热点 → 学习日记/学情日报 → 备课+热力图；同步准备 P0 材料（知识库 ≥50 条、学生花名册、心理数据源）。
3. **持续**：SQL 动态 WHERE 参数化；教师/教辅端 mock 换真实链路；上线前统一 err.Error() 规范并纳入 CI 扫描。

---

## 8. 综合评分与结论

### 8.1 评分总览

| 维度 | v3 | v4 | v5 | 变化 |
|------|----|----|----|------|
| 架构合规 | 9/10 | 8/10 | **9.5/10** | ↑1.5 — NEW-B1 修复，0 违规 |
| 安全审计 | 7/10 | 8/10 | **8.5/10** | ↑0.5 — W2~W5 修复，W1 残余+校验消息待清 |
| 知识管道 | 7/10 | 9/10 | **9/10** | — 维持 |
| 代码质量 | 7/10 | 7.5/10 | **8/10** | ↑0.5 — twin/teacher 修正，残余 handler 消息 |
| 基础设施 | — | 7/10 | **7/10** | — 维持 |
| 文档完整性 | 7/10 | 8/10 | **8.5/10** | ↑0.5 — 新增功能/材料双维审计基线 |
| **功能实现度**（新增） | — | — | **6/10** | 基线 — 40+ 项规划未落地，教师/教辅 mock |
| **上线材料就绪度**（新增） | — | — | **3/10** | 基线 — P0 数据源未就绪 |

> **核心工程维度均分**（架构/安全/知识管道/代码质量/基建/文档）：**8.4/10**（↑0.4，vs v4 8.0）  
> **含上线就绪度全维均分**（8 项）：**7.4/10**（新增两项拉低，如实反映上线准备不足）

### 8.2 修复路线图

```
v4 遗留 8 项
  ├─ NEW-B1/W2/W3/W4/W5/W6/W7   → ✅ 全部修复
  └─ NEW-W1（12 处）             → ⚠️ 部分修复（feedback 清零，6 文件残余 1.5h）

v5 新增问题
  ├─ 安全：NEW-V4 校验消息 err.Error()（2h）
  ├─ 文档：NEW-V1 capabilities 77→104（0.5h）
  ├─ 产品：NEW-V2 功能未实现 + NEW-V3 材料未就绪（规划级）
  └─ 质量：NEW-V5 assistant mock + SQL 动态 WHERE（4h）
```

### 8.3 结论

> **整体评级：✅ 良好（核心工程 8.4/10；含上线就绪度 7.4/10）**  
> **v5 进步**：v4 遗留的架构阻断（NEW-B1）与 6 项安全/质量警告全部消除；知识管道维持满分；代码质量随 twin/teacher 规范化回升。当前代码库**无阻断级遗留**。  
> **v5 新发现**：① 能力授权文档滞后（77 vs 104）；② 规划中 40+ 项 AI 原生功能仍未实现，教师/教辅端为 mock；③ 上线材料 P0 数据源全部未就绪。  
> **剩余工作**：安全面约 4h（NEW-W1 残余 + NEW-V4 校验消息）；产品与材料为规划级投入，建议按「全局 TOP10 协同功能组」推进并同步补齐知识库（≥50 条）、花名册、心理/课表数据源。  
> **上线建议**：以「智能问答 + 知识库 + 学生导入 + 情感预警」构成 P0 最小闭环先行上线；教师/教辅 AI 功能在材料就绪前保持 mock 并显式标注 `data_source`，避免误判为已实现。

---

*报告生成：2026-08-02 | 审核工具：静态代码审计 + 架构扫描 + 安全模式匹配 + 知识管道验证 + 角色功能/材料文档对照核验*
