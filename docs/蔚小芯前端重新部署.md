# 蔚小芯前端重新部署 — Cloudflare Pages

> 因原域名 `pydaydayup.xyz` 过期且 Vercel CDN 中国大陆访问受阻，前端迁移至 Cloudflare Pages（免费，国内可访问）。

## 新架构

```
浏览器 → Cloudflare Pages (wxx-agent.pages.dev)
  ├── /*  → Flutter Web 静态文件（前端）
  └── /api/* → Pages Functions 代理 → Vercel Serverless（后端）
```

**前端地址**: `https://wxx-agent.pages.dev`
**后端地址**: `https://wxx-server-j1us8ki1c-czldl.vercel.app`（2026-07-23 发布，仅 Functions 直连，不对外暴露）

## 部署方式

### 方式一：GitHub Actions 自动部署（推荐）

每次 push 到 `main` 分支的 `frontend/` 目录时自动构建并部署。

**前置条件** — 在 GitHub 仓库添加 2 个 Secrets：

> `Settings` → `Secrets and variables` → `Actions` → `New repository secret`

| Secret 名称 | 值 |
|---|---|
| `CLOUDFLARE_API_TOKEN` | 在 Cloudflare 创建 Pages 部署令牌后写入 GitHub Secrets，禁止提交到仓库 |
| `CLOUDFLARE_ACCOUNT_ID` | `6d2dbc6ca6fc4df401a93e01b268728b` |

工作流文件：`.github/workflows/deploy-frontend.yml`

### 方式二：手动部署

```bash
cd frontend

# 1. 构建
flutter build web --release

# 2. 准备部署目录（静态文件 + API 代理函数）
mkdir -p deploy
cp -r build/web/* deploy/
cp -r functions deploy/

# 3. 部署到 Cloudflare Pages
npx wrangler pages deploy deploy --project-name wxx-agent --branch main
```

## 项目文件说明

| 文件 | 作用 |
|---|---|
| `frontend/functions/api/[[route]].js` | Pages Functions — 将 `/api/*` 请求转发到 Vercel 后端 |
| `.github/workflows/deploy-frontend.yml` | GitHub Actions 自动部署 |
| `frontend/lib/config/api_config.dart` | API 地址配置（当前为空字符串，走同域代理） |

## API 代理原理

`frontend/functions/api/[[route]].js` 是一个 Cloudflare Pages Function，作用：

1. 前端发起请求 `/api/v1/auth/login`
2. Pages 匹配到 `functions/api/[[route]].js`
3. 转发到 `https://wxx-server-j1us8ki1c-czldl.vercel.app/api/v1/auth/login`
4. 返回结果给前端（添加 CORS 头）

**优点**：
- 前后端同域，无 CORS 问题
- 利用 Cloudflare 中国边缘节点加速
- 无需单独维护 Workers 项目

## Vercel 后端仍保留

后端 Go 服务仍然部署在 Vercel Serverless（`wxx-server` 项目），域名 `api.pydaydayup.xyz` 已过期不可用，但 `.vercel.app` 原生域名仍可通过 Cloudflare Functions 代理访问。

如需重新部署后端：

```bash
cd server
go build ./...
# 从仓库根目录
npx vercel deploy --prod
```

> 后端 Vercel 项目：`wxx-server`（`czldl/wxx-server`），仓库根目录 `.vercel/repo.json` 指向此项目。

## 2026-07-23 发布记录

- Vercel 后端生产部署：`wxx-server-j1us8ki1c-czldl.vercel.app`，状态 `READY`。
- Cloudflare Pages Function 已切换到上述后端地址。
- 本地发布前验收：Go 相关测试、`go build ./...`、Flutter Web Release 构建及学生 Excel 导入 E2E 均通过。
- Cloudflare Pages 线上验收：`wxx-agent.pages.dev` 页面与标题正常；管理员登录 `200`、空密码 `400`、错误密码 `401`；学生导入请求 `403`；管理员导入参考表 `5/5` 成功；导入学生可使用学号与初始密码登录。
- 登录页不再显示“开发环境可留空”；Pages 对入口与主程序禁用旧版本缓存，登录代理对瞬时上游断连重试一次，并将后端认证消息准确展示给用户。
- Web 固定使用 HTML 渲染器，直接使用浏览器系统中文字体，避免 CanvasKit 字体回退导致正文乱码；`/api/health` 可检查 Vercel 后端状态，资料与权限启动接口遇到瞬时断连会自动重试一次。
- 反馈截图在浏览器端缩放到 960×540 以内并限制为 900KB，避免 Base64 请求体导致连接超时；客户端连接/响应超时分别放宽到 90/120 秒，界面不再展示原始 Dio 异常；Cloudflare 代理对反馈提交完整缓冲上游响应，避免浏览器收到中途断流。反馈提交属于非幂等写操作，代理不自动重试，以免重复创建。
- Cloudflare API Token 曾出现在本文档中，现已移除；应在 Cloudflare 控制台轮换旧令牌，并只通过 GitHub Secrets 或本机环境变量使用。

## 旧 Vercel 前端部署记录

原 Vercel 前端项目 `wxx-frontend`（`czldl/wxx-frontend`）及域名 `wxx.pydaydayup.xyz` 已停用。旧部署流程保留在 `docs/deployment.md` 中。
