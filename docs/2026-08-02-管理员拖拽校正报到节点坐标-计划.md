# 管理员拖拽校正报到节点坐标 — 实现计划

> **目标：** 管理员登录后在校园地图上拖拽报到节点即可校正坐标，保存后全员可见，确保节点与实地一致。

**架构：** 后端新增 `PATCH /admin/campus/steps/:id/coords` 接口（无 draft 限制直接更新坐标）；前端 `campus_map_page.dart` 改为从 `GET /campus/steps` 加载步骤，管理员显示「编辑节点」按钮，进入编辑模式后拖拽标注即调用 PATCH 保存。

**技术栈：** Go/Gin + SQLite（后端）；Flutter Web + BMapGL iframe postMessage（前端）。

---

## 文件清单

| 文件 | 操作 | 职责 |
|------|------|------|
| `server/internal/repository/campus_repository.go` | 修改 | 新增 `UpdateCoords(id,lat,lng)` 方法，无 status 限制 |
| `server/internal/handler/campus_handler.go` | 修改 | 新增 `UpdateStepCoords` handler |
| `server/pkg/app/app.go` | 修改 | 注册 `PATCH /admin/campus/steps/:id/coords` 路由 |
| `server/migrations/050_fix_campus_step_coords.sql` | 新建 | 修正 048 种子数据中的错误坐标为 OSM 权威值 |
| `frontend/lib/config/api_config.dart` | 修改 | 新增 `adminCampusStepCoords(id)` 常量 |
| `frontend/lib/pages/campus/campus_map_page.dart` | 修改 | 加载后端步骤、管理员编辑模式、拖拽保存 |
| `docs/蔚小芯报到节点坐标管理.md` | 新建 | 功能说明文档 |

---

## Task 1：后端 — 新增坐标更新仓库方法

**Files:**
- Modify: `server/internal/repository/campus_repository.go`

- [ ] **Step 1：在 `campus_repository.go` 末尾追加 `UpdateCoords` 方法**

```go
// UpdateCoords 仅更新坐标（管理员拖拽校正专用，不受 draft 状态限制）
// 用于已发布步骤的实地位置微调，不改其他字段，不走审核流程。
func (r *CampusRepository) UpdateCoords(id int64, lat, lng float64) error {
	res, err := r.db.Exec(
		`UPDATE campus_checkin_steps
		SET lat=?,lng=?,updated_at=?
		WHERE id=?`,
		lat, lng, time.Now().Format(time.DateTime), id,
	)
	if err != nil {
		return err
	}
	n, _ := res.RowsAffected()
	if n == 0 {
		return fmt.Errorf("步骤 %d 不存在", id)
	}
	return nil
}
```

- [ ] **Step 2：编译验证**

Run: `cd server && go build ./...`
Expected: 编译通过，无错误。

---

## Task 2：后端 — 新增坐标更新 Handler

**Files:**
- Modify: `server/internal/handler/campus_handler.go`

- [ ] **Step 1：在 `campus_handler.go` 追加 `UpdateStepCoords` handler**

```go
// UpdateStepCoords PATCH /api/v1/admin/campus/steps/:id/coords
// 管理员拖拽校正节点坐标（不受 draft 状态限制）
func (h *CampusHandler) UpdateStepCoords(c *gin.Context) {
	id, err := strconv.ParseInt(c.Param("id"), 10, 64)
	if err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"code": 400, "message": "无效的步骤 ID"})
		return
	}
	var req struct {
		Lat float64 `json:"lat" binding:"required"`
		Lng float64 `json:"lng" binding:"required"`
	}
	if err := c.ShouldBindJSON(&req); err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"code": 400, "message": "参数校验失败: " + err.Error()})
		return
	}
	// 坐标合理性校验：经纬度必须在中国大致范围内
	if req.Lat < 3 || req.Lat > 54 || req.Lng < 73 || req.Lng > 136 {
		c.JSON(http.StatusBadRequest, gin.H{"code": 400, "message": "坐标超出中国范围，拒绝保存"})
		return
	}
	if err := h.repo.UpdateCoords(id, req.Lat, req.Lng); err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"code": 400, "message": err.Error()})
		return
	}
	c.JSON(http.StatusOK, gin.H{"code": 0, "message": "坐标已更新"})
}
```

- [ ] **Step 2：编译验证**

Run: `cd server && go build ./...`
Expected: 编译通过。

---

## Task 3：后端 — 注册路由

**Files:**
- Modify: `server/pkg/app/app.go:862-871`

- [ ] **Step 1：在 campusAdmin 路由组追加坐标更新路由**

在 `campusAdmin.DELETE("/steps/:id", ...)` 之前插入：

```go
				// 管理员拖拽校正坐标（college_admin+，不走审核流程）
				campusAdmin.PATCH("/steps/:id/coords", auth.RequireCapability(auth.CollegeUserRead), campusH.UpdateStepCoords)
```

- [ ] **Step 2：编译并启动服务器验证路由注册**

Run: `cd server && go build -o /tmp/wxx_test ./cmd/server`
Expected: 编译通过。

---

## Task 4：后端 — 修正种子数据坐标

**Files:**
- Create: `server/migrations/050_fix_campus_step_coords.sql`

- [ ] **Step 1：创建迁移文件，将 12 个步骤坐标更新为 OSM 权威值**

```sql
-- 修正 048 种子数据中的错误坐标（会峰/琅琊写反、琅琊偏移 2km）
-- 数据来源：OpenStreetMap way 734826227（会峰）、734826234（琅琊）
-- 会峰校区中心 32.2743/118.3055，琅琊校区中心 32.2943/118.2978

-- 会峰校区 6 个步骤（原坐标指向琅琊位置，现纠正到会峰校区内）
UPDATE campus_checkin_steps SET lat=32.2705, lng=118.3055, updated_at=datetime('now')
  WHERE campus_id='huifeng' AND step_order=1;
UPDATE campus_checkin_steps SET lat=32.2745, lng=118.3070, updated_at=datetime('now')
  WHERE campus_id='huifeng' AND step_order=2;
UPDATE campus_checkin_steps SET lat=32.2735, lng=118.3060, updated_at=datetime('now')
  WHERE campus_id='huifeng' AND step_order=3;
UPDATE campus_checkin_steps SET lat=32.2770, lng=118.3040, updated_at=datetime('now')
  WHERE campus_id='huifeng' AND step_order=4;
UPDATE campus_checkin_steps SET lat=32.2740, lng=118.3090, updated_at=datetime('now')
  WHERE campus_id='huifeng' AND step_order=5;
UPDATE campus_checkin_steps SET lat=32.2720, lng=118.3030, updated_at=datetime('now')
  WHERE campus_id='huifeng' AND step_order=6;

-- 琅琊校区 6 个步骤（原坐标偏移到校区以北 2km，现纠正到琅琊校区内）
UPDATE campus_checkin_steps SET lat=32.2921, lng=118.2988, updated_at=datetime('now')
  WHERE campus_id='langya' AND step_order=1;
UPDATE campus_checkin_steps SET lat=32.2932, lng=118.3002, updated_at=datetime('now')
  WHERE campus_id='langya' AND step_order=2;
UPDATE campus_checkin_steps SET lat=32.2928, lng=118.2995, updated_at=datetime('now')
  WHERE campus_id='langya' AND step_order=3;
UPDATE campus_checkin_steps SET lat=32.2940, lng=118.2976, updated_at=datetime('now')
  WHERE campus_id='langya' AND step_order=4;
UPDATE campus_checkin_steps SET lat=32.2926, lng=118.3000, updated_at=datetime('now')
  WHERE campus_id='langya' AND step_order=5;
UPDATE campus_checkin_steps SET lat=32.2917, lng=118.2992, updated_at=datetime('now')
  WHERE campus_id='langya' AND step_order=6;
```

- [ ] **Step 2：确认迁移会被自动执行（runMigrations 按 filename 排序，050 > 049）**

无需代码改动，`app.go:runMigrations` 会自动读取 `migrations/` 目录下所有 .sql 按序执行。

---

## Task 5：前端 — 新增 API 配置

**Files:**
- Modify: `frontend/lib/config/api_config.dart:342-350`

- [ ] **Step 1：在校园报到步骤 API 区块追加坐标更新接口常量**

在 `adminCampusStepPublish` 之后追加：

```dart
  static String adminCampusStepCoords(String id) =>
      '$apiPrefix/admin/campus/steps/$id/coords';
```

- [ ] **Step 2：编译验证**

Run: `cd frontend && flutter analyze lib/config/api_config.dart`
Expected: 无错误。

---

## Task 6：前端 — campus_map_page.dart 改为从后端加载步骤

**Files:**
- Modify: `frontend/lib/pages/campus/campus_map_page.dart`

- [ ] **Step 1：将 `_huifengSteps`/`_langyaSteps`/`_campuses` 从 `const` 改为可变 `final`，并加 `_` 前缀使其可在运行时被覆盖**

把 `const _huifengSteps = [...]` 改为 `final _huifengSteps = [...]`，`const _langyaSteps` 同理，`const _campuses` 改为 `final _campuses`。

- [ ] **Step 2：在 `_CampusMapPageState` 中新增加载后端步骤的逻辑**

在类顶部新增字段和方法：

```dart
  final ApiService _api = ApiService();
  bool _loadingSteps = false;
  String? _stepsError;
  // 编辑模式：管理员拖拽校正节点坐标
  bool _editMode = false;
  // 拖拽中临时保存的坐标变更（index → (lat,lng)），未保存前不写回 _steps
  final Map<int, _CheckinStep> _pendingEdits = {};

  /// 判断当前用户是否有权编辑节点坐标（sys/school/college_admin）
  bool get _canEditNodes {
    final role = Storage.role ?? 'student';
    return role == 'sys_admin' ||
        role == 'school_admin' ||
        role == 'college_admin';
  }

  @override
  void initState() {
    super.initState();
    _loadStepsFromServer();
  }

  /// 从后端加载报到步骤（公开接口，无需登录）
  /// 失败时回退到本地硬编码常量，保证离线可用
  Future<void> _loadStepsFromServer() async {
    setState(() {
      _loadingSteps = true;
      _stepsError = null;
    });
    try {
      final resp = await _api.get('${ApiConfig.campusSteps}?campus=${_campus.id}');
      if (resp.data['code'] == 0) {
        final list = resp.data['data'] as List? ?? [];
        if (list.isNotEmpty) {
          final loaded = list
              .map((e) => _CheckinStep(
                    title: e['title'] ?? '',
                    location: e['location'] ?? '',
                    lat: (e['lat'] as num?)?.toDouble() ?? 0.0,
                    lng: (e['lng'] as num?)?.toDouble() ?? 0.0,
                    duration: e['duration'] ?? '',
                    task: e['task'] ?? '',
                    materials: e['materials'] ?? '',
                    contact: e['contact'] ?? '',
                    note: e['note'] ?? '',
                    icon: _iconFromName(e['icon_name'] ?? 'place'),
                  ))
              .toList();
          // 用后端数据替换当前校区的步骤
          _campusStepsMap[_campus.id] = loaded;
          setState(() {
            _loadingSteps = false;
          });
          return;
        }
      }
      setState(() => _loadingSteps = false);
    } catch (e) {
      // 静默回退到硬编码常量
      setState(() {
        _loadingSteps = false;
        _stepsError = null;
      });
    }
  }
```

并在类中新增步骤映射表和图标解析辅助：

```dart
  /// 各校区步骤的可变副本（后端加载后覆盖）
  final Map<String, List<_CheckinStep>> _campusStepsMap = {
    'huifeng': _huifengSteps,
    'langya': _langyaSteps,
  };

  /// icon_name 字符串 → Material IconData
  IconData _iconFromName(String name) {
    switch (name) {
      case 'login': return Icons.login;
      case 'account_balance': return Icons.account_balance;
      case 'payments': return Icons.payments_outlined;
      case 'bed': return Icons.bed_outlined;
      case 'credit_card': return Icons.credit_card;
      case 'health_and_safety': return Icons.health_and_safety_outlined;
      default: return Icons.place;
    }
  }
```

修改 `_steps` getter：

```dart
  List<_CheckinStep> get _steps => _campusStepsMap[_campus.id] ?? _campus.steps;
```

- [ ] **Step 3：切换校区时重新加载步骤**

在 `_switchCampus` 方法中追加：

```dart
  void _switchCampus(int index) {
    setState(() {
      _campusIndex = index;
      _currentStep = 0;
      _pendingEdits.clear();
    });
    _loadStepsFromServer();
  }
```

- [ ] **Step 4：编译验证**

Run: `cd frontend && flutter analyze lib/pages/campus/campus_map_page.dart`
Expected: 无错误（可能有 info 级提示，忽略）。

---

## Task 7：前端 — 管理员编辑按钮 + 拖拽保存

**Files:**
- Modify: `frontend/lib/pages/campus/campus_map_page.dart`

- [ ] **Step 1：在 `_buildCampusMapCanvas` 中给 `BaiduCampusMapEmbed` 传入 editMode 和 onMarkerMoved**

```dart
          Positioned.fill(
            child: BaiduCampusMapEmbed(
              baiduAk: baiduAk,
              amapAk: '',
              tencentAk: '',
              provider: 'baidu',
              steps: _stepsForMap,
              currentStep: _currentStep,
              campusId: _campus.id,
              editMode: _editMode,
              controller: _mapController,
              onStepSelected: (idx) => setState(() => _currentStep = idx),
              onMarkerMoved: _canEditNodes ? _onMarkerMoved : null,
            ),
          ),
```

- [ ] **Step 2：新增 `_onMarkerMoved` 方法保存坐标**

```dart
  /// 管理员拖拽标注后，调用后端接口保存新坐标
  Future<void> _onMarkerMoved(int index, double lat, double lng) async {
    if (!_canEditNodes) return;
    final step = _steps[index];
    // 先在本地立即更新（避免拖拽后视觉回弹）
    final updated = _CheckinStep(
      title: step.title,
      location: step.location,
      lat: lat,
      lng: lng,
      duration: step.duration,
      task: step.task,
      materials: step.materials,
      contact: step.contact,
      note: step.note,
      icon: step.icon,
    );
    final list = List<_CheckinStep>.from(_steps);
    list[index] = updated;
    _campusStepsMap[_campus.id] = list;
    _pendingEdits[index] = updated;
    setState(() {});

    // 调用后端保存
    try {
      final stepId = _remoteStepId(index);
      if (stepId == null) {
        // 后端无对应记录（本地回退模式），仅本地保存
        return;
      }
      final resp = await _api.patch(
        ApiConfig.adminCampusStepCoords(stepId.toString()),
        data: {'lat': lat, 'lng': lng},
      );
      if (resp.data['code'] == 0) {
        if (mounted) {
          ScaffoldMessenger.of(context).showSnackBar(
            const SnackBar(
              content: Text('节点坐标已保存'),
              duration: Duration(seconds: 2),
            ),
          );
        }
      }
    } catch (e) {
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(
            content: Text('保存失败：$e'),
            duration: const Duration(seconds: 3),
          ),
        );
      }
    }
  }

  /// 获取当前校区第 index 个步骤的后端 ID（若来自后端则缓存，否则 null）
  /// 简化实现：加载后端步骤时把 ID 存入 _remoteIds
  final Map<int, int> _remoteIds = {}; // index → remote id

  int? _remoteStepId(int index) => _remoteIds[index];
```

在 `_loadStepsFromServer` 的 `loaded` 映射中同时缓存 id：

```dart
          // 同时缓存后端 ID，供拖拽保存使用
          _remoteIds.clear();
          for (var i = 0; i < list.length; i++) {
            final id = (list[i] is Map ? list[i]['id'] : null);
            // 注意：这里需要把原始 JSON 的 id 也存下来
          }
```

修正为在 `list.map` 时把 id 一并保存（改用 `asMap` 遍历）：

```dart
        final loaded = <_CheckinStep>[];
        _remoteIds.clear();
        for (var i = 0; i < list.length; i++) {
          final e = list[i] as Map<String, dynamic>;
          loaded.add(_CheckinStep(
            title: e['title'] ?? '',
            location: e['location'] ?? '',
            lat: (e['lat'] as num?)?.toDouble() ?? 0.0,
            lng: (e['lng'] as num?)?.toDouble() ?? 0.0,
            duration: e['duration'] ?? '',
            task: e['task'] ?? '',
            materials: e['materials'] ?? '',
            contact: e['contact'] ?? '',
            note: e['note'] ?? '',
            icon: _iconFromName(e['icon_name'] ?? 'place'),
          ));
          final id = e['id'];
          if (id is num) _remoteIds[i] = id.toInt();
        }
```

- [ ] **Step 3：在顶部控件栏添加「编辑节点」按钮（仅管理员可见）**

在 `_buildMobileTopBar` 和 `_buildDesktopTopBar` 的 Wrap/Row 中追加：

```dart
          if (_canEditNodes)
            FilterChip(
              selected: _editMode,
              label: Text(_editMode ? '退出编辑' : '编辑节点',
                  style: const TextStyle(fontSize: 11)),
              avatar: Icon(Icons.edit_location_alt,
                  size: 14,
                  color: _editMode ? Colors.white : theme.colorScheme.primary),
              onSelected: (v) {
                setState(() => _editMode = v);
                if (v) {
                  ScaffoldMessenger.of(context).showSnackBar(
                    const SnackBar(
                      content: Text('编辑模式：拖动标注即可校正位置，松手自动保存'),
                      duration: Duration(seconds: 3),
                    ),
                  );
                }
              },
              visualDensity: VisualDensity.compact,
            ),
```

- [ ] **Step 4：编译验证**

Run: `cd frontend && flutter analyze lib/pages/campus/campus_map_page.dart`
Expected: 无错误。

---

## Task 8：文档 + 提交推送

**Files:**
- Create: `docs/蔚小芯报到节点坐标管理.md`

- [ ] **Step 1：编写功能说明文档**

文档内容：管理员登录后地图工具栏出现「编辑节点」按钮，点击进入编辑模式，拖拽标注到实地位置松手即自动保存，所有用户刷新后看到最新坐标；数据流：Flutter → PATCH /admin/campus/steps/:id/coords → SQLite campus_checkin_steps 表。

- [ ] **Step 2：Git 提交推送**

```bash
git add server/internal/repository/campus_repository.go \
  server/internal/handler/campus_handler.go \
  server/pkg/app/app.go \
  server/migrations/050_fix_campus_step_coords.sql \
  frontend/lib/config/api_config.dart \
  frontend/lib/pages/campus/campus_map_page.dart \
  docs/蔚小芯报到节点坐标管理.md
git commit -m "feat(campus): 管理员拖拽校正报到节点坐标

- 后端新增 PATCH /admin/campus/steps/:id/coords 接口（无 draft 限制）
- 前端从 GET /campus/steps 加载步骤，失败回退硬编码常量
- 管理员(sys/school/college_admin)显示「编辑节点」按钮
- 拖拽标注松手自动保存，Snack 提示结果
- 迁移 050 修正 048 种子数据错误坐标为 OSM 权威值"
git push origin main
```

---

## Task 9：部署验证

- [ ] **Step 1：后端部署到服务器**

```bash
cd server && GOOS=linux GOARCH=amd64 go build -o /tmp/wxx_server ./cmd/server
scp -i ~/.ssh/wxx_deploy.pem /tmp/wxx_server root@129.211.223.113:/opt/wxx/wxx_server
ssh -i ~/.ssh/wxx_deploy.pem root@129.211.223.113 "systemctl restart wxx"
```

- [ ] **Step 2：前端构建部署**

```bash
cd frontend
flutter build web --release --web-renderer canvaskit \
  --dart-define=BAIDU_MAP_AK=OUouSU6WbYExGTlnDEFqqruhTH60KAwO \
  --dart-define=GAODE_MAP_AK=a2f48050b8ec16aca88db4d25c035fe6 \
  --dart-define=TENXUN_MAP_AK=E5IBZ-ZSUC3-EQN3G-R2B5G-A7H4J-TQFIR
# 打包上传部署（参考已有 deploy-frontend.sh 流程）
```

- [ ] **Step 3：浏览器验证**

1. 访问 `http://129.211.223.113:8080/home?v=fresh14#/campus`
2. 确认两个校区步骤坐标正确（会峰在丰乐大道、琅琊在琅琊西路）
3. 管理员登录后确认「编辑节点」按钮出现
4. 拖拽一个标注到新位置，确认 Snack 提示「节点坐标已保存」
5. 刷新页面确认坐标已持久化
