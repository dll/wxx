# Harness 工作流 — 与蔚小芯对齐

> 完整理论与清单见仓库 `学工/` 目录下（与 `WXX/` 同级）的 **`Harness驾驭工程规范.md`**。

## 本项目最小清单

1. 维护 `WXX/AGENTS.md` 与 **`docs/蔚小芯开发规范.md`**。  
2. **Plan → 评审 → 开发**；禁止大块需求无方案直接改代码。  
3. 配 MCP/Skills（开发机）用于文档与验证；生产合规另行评审。  
4. **每增量**：测试/Lint + **Git 提交** + 更新相关 `docs/` 或总纲附录。  
5. **架构护栏**：分层清晰、Pre-commit、禁止静默引入禁用依赖（本地模型、Coze、强制容器集群等）。

## 与 Context Engine 的衔接

Harness 解决「**如何稳定交付**」；Context Engine 解决「**答什么、凭什么答**」。二者并列：编排层（Eino）须遵守触发策略与 `sources` 硬约束。
