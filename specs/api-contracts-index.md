# API 契约索引

> 契约正文位于 **`docs/蔚小芯智能体.md`**（相对 `WXX/`）附录 A（后端 API 契约开发版）、附录 B（接口清单待确认）。

## 使用方式

1. 开发前在总纲附录中锁定 **路径、请求/响应字段、错误码**。  
2. 实现变更时在本文件追加 **变更条目**（日期、附录章节、摘要、兼容策略）。

## 变更记录（模板）

| 日期 | 附录位置 | 变更摘要 | 兼容策略 |
|------|----------|----------|----------|
| 2026-05-05 | 附录 A.2 | 新增 `DELETE /api/v1/sessions/:id` 删除会话接口 | 向前兼容，不影响现有接口 |
| 2026-05-05 | 附录 A.2 | 补充会话管理接口：`GET /sessions`、`GET /sessions/:id/messages` | 向前兼容 |
| 2026-05-04 | 附录 A.2 | 新增 `POST /api/v1/voice/asr` 语音识别接口（接收音频文件，返回识别文本） | 向前兼容 |
| 2026-05-04 | 附录 A.2 | 新增 `POST /api/v1/voice/tts` 语音合成接口（接收文本，返回 MP3 音频） | 向前兼容 |
| 2026-05-05 | 附录 A.2 | 新增 `GET /api/v1/knowledge?type=` 知识大厅公共浏览接口（所有已认证用户，按类型分组） | 向前兼容 |
| 2026-05-05 | 附录 A.2 | 新增 `POST /api/v1/emotion/analyze` 情感分析接口（支持手动触发 LLM 情感分析） | 向前兼容 |
| 2026-05-05 | 附录 A.2 | 新增 `GET /api/v1/emotion/alerts` 告警列表接口（分页 + 风险等级/状态过滤 + 角色范围隔离） | 向前兼容 |
| 2026-05-05 | 附录 A.2 | 新增 `PUT /api/v1/emotion/alerts/:id` 更新告警状态接口（确认/已处理） | 向前兼容 |

（首个正式版本发布后在此登记。）
